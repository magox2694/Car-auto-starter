package com.example.carbeats.search

class YouTubeMetadataProvider : TrackSearchProvider {
    override fun search(query: String): List<TrackSearchResult> {
        // Placeholder provider for future official YouTube metadata integration.
        // Use only official APIs and honor platform terms and content rights.
        return emptyList()
    }
}
